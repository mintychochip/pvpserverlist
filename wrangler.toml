# Cloudflare Pages configuration
# For Pages, the functions/ directory is automatically discovered
# No 'main' field needed - this is NOT a Worker project

name = "guildpost"
pages_build_output_dir = "dist"
compatibility_date = "2024-04-06"
compatibility_flags = ["nodejs_compat"]
account_id = "74c3a136217326e88564790be3373469"

[vars]
SUPABASE_URL = "https://wpxutsdbiampnxfgkjwq.supabase.co"

# Note: Secrets are set via Cloudflare Dashboard or wrangler secret put
# - SUPABASE_SERVICE_KEY
# - JINA_API_KEY (for semantic search embeddings - 768 dims)
# - PINECONE_API_KEY (for vector search)
# - CRON_SECRET
# - STRIPE_SECRET_KEY
# - STRIPE_WEBHOOK_SECRET

# Cron polling is handled via external cron service calling /api/cron/ping
