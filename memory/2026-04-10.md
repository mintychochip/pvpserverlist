---

## Proactive Run - API Routes Consolidation (7:10 PM)

### Problem Identified
API routes (`/api/search/semantic`, `/api/servers`, etc.) returning 404. Root cause: conflicting routing between Astro API routes and Cloudflare Functions.

### Analysis
- `_routes.json` had `/api/**` excluded → Functions should handle APIs
- But Functions didn't have all routes (hybrid search, wizard chat were only in Astro)
- Test expected `/api/**` NOT excluded (designed for Astro-only approach)

### Solution
Consolidated all API routes into Cloudflare Functions:
1. Added `handleHybridSearch()` - keyword + semantic search with Pinecone
2. Added `handleWizardChat()` - Gemma AI chat with hybrid search fallback
3. Updated route handlers in `onRequest()` for `/search/hybrid` and `/wizard/chat`
4. Updated `public/_routes.json` to exclude `/api/**`
5. Updated test to expect `/api/**` exclusion

### Files Modified
- `functions/api/[[path]].js` - Added hybrid search & wizard chat handlers
- `public/_routes.json` - Added `/api/**` exclusion
- `package.json` - Updated build script to copy _routes.json to project root
- `__tests__/routes.test.ts` - Updated test expectations

### APIs Now in Functions
- `/search/semantic` - Semantic search with embeddings
- `/search/hybrid` - NEW: Hybrid keyword + semantic search
- `/search/suggestions` - AI suggestions via Gemma
- `/wizard/chat` - NEW: AI wizard chat
- `/servers` - Server list
- `/ping` - Server ping
- `/cron/ping` - Batch ping cron job
- `/embeddings/batch` - Batch embedding generation
- `/servers/:id/edit` - Server edit
- `/servers/:id/posts` - Server posts
- `/servers/:id/analytics` - Server analytics

### Next Steps
- Deploy to Cloudflare Pages: `wrangler pages deploy . --project-name=guildpost --directory=dist`
- Verify APIs work after deployment
- Remove duplicate Astro API routes once Functions are confirmed working

[proactive] Mode A — Consolidated API routes into Cloudflare Functions — Tests passing, ready to deploy

---

[proactive] Mode A — Debug API routing — 11:09 PM

GET /api/servers still returning 404 HTML instead of JSON. Added debug logging to trace the route matching in the Cloudflare Function:
- Commit: f1689f3 — debug: add logging to trace servers route matching
- Pushed to trigger fresh Cloudflare Pages deployment
- Need to wait for deployment (~1-2 min) then re-test

Strike counter: 0
