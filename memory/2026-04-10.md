---

## Proactive Run - API Routes Consolidation (7:10 PM)

### Problem Identified
API routes (`/api/search/semantic`, `/api/servers`, etc.) returning 404. Root cause: conflicting routing between Astro API routes and Cloudflare Functions.

### Analysis
- `_routes.json` had `/api/**` excluded → Functions should handle APIs
- But Functions didn't have all routes (hybrid search, wizard chat were only in Astro)
- Test expected `/api/**` NOT excluded (designed for Astro-only approach)

### Solution
Consolidated all API routes into Cloudflare Functions:
1. Added `handleHybridSearch()` - keyword + semantic search with Pinecone
2. Added `handleWizardChat()` - Gemma AI chat with hybrid search fallback
3. Updated route handlers in `onRequest()` for `/search/hybrid` and `/wizard/chat`
4. Updated `public/_routes.json` to exclude `/api/**`
5. Updated test to expect `/api/**` exclusion

### Files Modified
- `functions/api/[[path]].js` - Added hybrid search & wizard chat handlers
- `public/_routes.json` - Added `/api/**` exclusion
- `package.json` - Updated build script to copy _routes.json to project root
- `__tests__/routes.test.ts` - Updated test expectations

### APIs Now in Functions
- `/search/semantic` - Semantic search with embeddings
- `/search/hybrid` - NEW: Hybrid keyword + semantic search
- `/search/suggestions` - AI suggestions via Gemma
- `/wizard/chat` - NEW: AI wizard chat
- `/servers` - Server list
- `/ping` - Server ping
- `/cron/ping` - Batch ping cron job
- `/embeddings/batch` - Batch embedding generation
- `/servers/:id/edit` - Server edit
- `/servers/:id/posts` - Server posts
- `/servers/:id/analytics` - Server analytics

### Next Steps
- Deploy to Cloudflare Pages: `wrangler pages deploy . --project-name=guildpost --directory=dist`
- Verify APIs work after deployment
- Remove duplicate Astro API routes once Functions are confirmed working

[proactive] Mode A — Consolidated API routes into Cloudflare Functions — Tests passing, ready to deploy

---

[proactive] Mode A — Debug API routing — 11:09 PM

GET /api/servers still returning 404 HTML instead of JSON. Added debug logging to trace the route matching in the Cloudflare Function:
- Commit: f1689f3 — debug: add logging to trace servers route matching
- Pushed to trigger fresh Cloudflare Pages deployment
- Need to wait for deployment (~1-2 min) then re-test

Strike counter: 0

---

# GuildPost Proactive Run - April 10, 2026 (9:10 AM)

## Summary
[proactive] Mode A — Fixed API server data issue — Committed and pushed, deployment pending

## Problem Found
The homepage showed "0 Servers" / "Loading servers..." indefinitely. The API was working (returning 200), but:
1. The API only returned `id,host,port,game_mode` fields
2. The frontend expected `ip, name, status, players_online, vote_count, tags`, etc.
3. The field was named `host` but frontend expected `ip`

## Fixes Applied

### Commit dc7f4d4 - Fix API server data response
**File:** `functions/api/[[path]].js`
- Changed Supabase query from `select=id,host,port,game_mode` to `select=*`
- Added mapping: `host -> ip` for frontend compatibility

**File:** `package.json`
- Fixed build script to copy `functions/` folder to `dist/` for deployment
- Previous script: `astro build && cp public/_routes.json dist/_routes.json && cp public/_routes.json _routes.json`
- New script: added `&& cp -r functions dist/`

## Deployment Status
- Git commit pushed to master (dc7f4d4)
- Cloudflare Pages should auto-deploy
- Need to verify deployment completes and site shows servers

## Next Steps
1. Monitor deployment at https://guildpost.tech
2. Verify homepage displays server count (should be ~1000+)
3. Check that featured servers section loads correctly

---

# GuildPost Proactive Run - April 10, 2026 (9:41 AM)

## Summary
[proactive] Mode A — Fixed _routes.json API exclusion — Committed and pushed

## Problem Found
Featured servers section still showing "Loading servers..." despite API working correctly via curl. Root cause: `_routes.json` had `"exclude": []` instead of `["/api/*"]`, so Cloudflare Pages was trying to serve API routes as static files (which don't exist) instead of routing them to Cloudflare Functions.

## Fixes Applied

### Commit 581095d - Fix _routes.json to exclude /api/*
**File:** `public/_routes.json`
- Changed `"exclude": []` to `"exclude": ["/api/*"]`
- This tells Cloudflare Pages to route API requests to Functions instead of looking for static files

**File:** `_routes.json` (root)
- Copied updated version to project root for wrangler deployment

**File:** `__tests__/routes.test.ts`
- Updated tests to expect `/api/*` in exclude array
- Tests pass ✓

## Deployment Status
- Git commit pushed to master (581095d)
- Cloudflare Pages auto-deploy should trigger shortly
- Once deployed, `/api/servers` will route to Functions instead of 404ing

## Next Steps
1. Wait ~2 minutes for Cloudflare Pages deployment
2. Verify featured servers load on homepage
3. If successful, the "Loading servers..." text should be replaced with actual server cards

---
